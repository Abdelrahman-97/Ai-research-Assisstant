# --- Core web framework ---
fastapi
uvicorn[standard]
python-multipart    # file uploads (multipart/form-data)

# --- Config & validation ---
pydantic
pydantic-settings
python-dotenv
email-validator     # EmailStr validation

# --- Auth ---
bcrypt              # password hashing (used directly; passlib is unmaintained)
pyjwt               # JWT access tokens

# --- Security ---
slowapi             # rate limiting

# --- Database ---
sqlalchemy          # ORM / engine (SQLite by default, Postgres in prod)
psycopg2-binary     # Postgres driver (only needed when using Postgres)

# --- Data handling ---
pandas
openpyxl            # .xlsx reading

# --- Scientific stack (used by the generated analysis scripts) ---
numpy
scipy               # statistical tests
statsmodels         # regression / ANOVA / mixed models
matplotlib          # figures (headless 'Agg' backend at runtime)

# --- Document export (Markdown -> .docx / .pdf) ---
python-docx
reportlab           # PDF generation

# --- AI / LLM ---
openai              # Kimi is OpenAI-compatible; used via base_url override

# --- Grammar checking (local, no API key) ---
language-tool-python

# --- Sandbox orchestration ---
docker              # controls the execution sandbox container

# --- Testing ---
pytest
httpx               # test client for FastAPI
